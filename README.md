# GMAT Question Timer

This version avoids Tailwind/PostCSS build issues by loading Tailwind through the browser CDN.

## Vercel settings

Build command:

```bash
npm run build
```

Output directory:

```bash
dist
```
